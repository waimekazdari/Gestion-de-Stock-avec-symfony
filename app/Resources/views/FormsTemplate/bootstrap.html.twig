{% block form_widget_simple %}
{% set type = type|default('text') %}
<input type="{{ type }}" {{ block('widget_attributes') }} {% if value is not empty
%}value="{{ value }}" {% endif %}/ class="form-control" style="width:300px">
{% endblock form_widget_simple %}